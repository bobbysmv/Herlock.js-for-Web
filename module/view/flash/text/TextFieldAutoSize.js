define([

],function(  ){

    var TextFieldAutoSize = {
        /** サイズ変更が発生しないことを指定します。 */
        NONE : "none",
        /** テキストが中央揃えテキストとして扱われることを指定します。 */
        CENTER : "center",
        /** テキストが左揃えテキストとして扱われることを指定します。 */
        LEFT : "left",
        /** テキストが右揃えテキストとして扱われることを指定します。 */
        RIGHT : "right"
    };

    return TextFieldAutoSize;
});